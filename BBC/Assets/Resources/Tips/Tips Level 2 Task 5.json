{
  "Items": [
    {
      "Tip": "...\n{\n    int pathNumber;\n    if (isFirstPathClear &&  ...)\n        ...\n}"
    },
    {
      "Tip": "...\n{\n    int pathNumber;\n    if (isFirstPathClear && !isSecondPathClear && !isThirdPathClear)\n        pathNumber = 1;\n        ...\n}"
    },
    {
      "Tip": "...\n{\n    int pathNumber;\n    if (isFirstPathClear && !isSecondPathClear && !isThirdPathClear)\n        pathNumber = 1;\n    else if (!isFirstPathClear && ...)\n        ...\n}"
    },
    {
      "Tip": "...\n{\n    int pathNumber;\n    if (isFirstPathClear && !isSecondPathClear && !isThirdPathClear)\n        pathNumber = 1;\n    else if (!isFirstPathClear && isSecondPathClear && !isThirdPathClear)\n        ...\n}"
    },
    {
      "Tip": "...\n{\n    int pathNumber;\n    if (isFirstPathClear && !isSecondPathClear && !isThirdPathClear)\n        pathNumber = 1;\n    else if (!isFirstPathClear && isSecondPathClear && !isThirdPathClear)\n        pathNumber = 2;\n    ...\n}"
    },
    {
      "Tip": "...\n{\n    int pathNumber;\n    if (isFirstPathClear && !isSecondPathClear && !isThirdPathClear)\n        pathNumber = 1;\n    else if (!isFirstPathClear && isSecondPathClear && !isThirdPathClear)\n        pathNumber = 2;\n    else pathNumber = 3;\n    return ...\n}"
    },
    {
      "Tip": "...\n{\n    int pathNumber;\n    if (isFirstPathClear && !isSecondPathClear && !isThirdPathClear)\n        pathNumber = 1;\n    else if (!isFirstPathClear && isSecondPathClear && !isThirdPathClear)\n        pathNumber = 2;\n    else pathNumber = 3;\n    return pathNumber * 400;\n}"
    }
  ]
}