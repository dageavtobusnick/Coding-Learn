{
  "Items": [
    {
      "Tip": "     1)Используйте foreach в связке с if. Введите вспомогательную переменную для хранения количества уже найденных вхождений itemToFind"
    },
    {
      "Tip": "     1)Используйте foreach в связке с if. Введите вспомогательную переменную для хранения количества уже найденных вхождений itemToFind\n     2)В этой задаче индексы не нужны, поэтому подойдёт foreach"
    },
    {
      "Tip": "     1)Используйте foreach в связке с if. Введите вспомогательную переменную для хранения количества уже найденных вхождений itemToFind\n     2)В этой задаче индексы не нужны, поэтому подойдёт foreach\n     3)Заведите ещё одну переменную, чтобы в ней считать ответ"
    }
  ]
}